const state = {
    sage:"23"
    //存数据

};  
const mutations = {
    //直接修改数据
};   
const actions = {
    //一般是异步发请求 提交mutations
};   
const getters = {
    //相当于计算属性
}; 

export default {
    state,
    mutations,actions,
    getters
}